[package]
name = "e2e-tests"
version = "0.1.0"
edition = "2021"
publish = false
description = "Opaque-box end-to-end tests driving the flashgui-cli binary against the mock and live backends"

[lib]
name = "e2e_harness"
path = "src/lib.rs"

[dependencies]
serde_json = "1.0"
tempfile = "3.10"

[dev-dependencies]
firmware-parser = { path = "../firmware-parser" }
